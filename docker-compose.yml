services:
  web:
    build:
      context: ./web
    container_name: lyrics-web
    ports:
      - "8000:8000"
    depends_on:
      - db
    environment:
      - DATABASE_URL=postgresql://postgres:postgres@db:5432/lyrics_db
  db:
    image: postgres:15
    container_name: lyrics-db
    environment:
      POSTGRES_PASSWORD: wSXAlI9oHujY3XmC8AqNjpjKaXuLt7HCP7TSnjyNNOSasgZZyqCWpMNn3Xmg1gC792
      POSTGRES_USER: ampache
      POSTGRES_DB: lyricsdb
    ports:
      - "5432:5432"
    volumes:
      - ./db_init:/docker-entrypoint-initdb.d


